# Regenerated by every run, and reproducible to the byte -- which is the
# property under test, so committing ~30MB of them would prove nothing that
# out/receipt.json does not already state.
out/*.png
