// Replaced with the exact checked-out commit by the edge deployment workflow.
// Compiled into each bundle, rather than read from a mutable project secret.
export const RELEASE_SOURCE_SHA = "development";
