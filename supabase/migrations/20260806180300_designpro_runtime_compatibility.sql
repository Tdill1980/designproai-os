-- Reserved migration number. The legacy runtime-compatibility views and RPCs
-- were intentionally removed once the standalone claimant and gateway port
-- became complete. No shared workforce/domain object is synthesized here.
SELECT 1;
