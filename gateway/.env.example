NODE_ENV=production
PORT=8787
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_REPLACE_ME
DESIGNPRO_APP_ORIGIN=https://os.designproai.com
DESIGNPRO_RUNTIME_INTERNAL_URL=http://runtime-1:3001
# Shared only between the loopback/Docker-internal gateway and runtime. This is
# not a Supabase key and is never sent to the browser or exposed through Caddy.
WORKER_SECRET=replace_with_at_least_32_random_characters
