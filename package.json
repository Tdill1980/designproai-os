{
  "name": "designproai-os",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Standalone server-owned DesignProAI production operating system",
  "type": "module",
  "scripts": {
    "test": "node scripts/run-all-tests.mjs",
    "test:supabase": "node --test tests/*.test.mjs",
    "test:gateway": "npm test --prefix gateway",
    "test:web": "npm test --prefix web",
    "test:ops": "bash ops/validate-package.sh",
    "build:app": "npm run build --prefix app",
    "build:web": "npm run build --prefix web",
    "test:shadow": "bash scripts/test-supabase-shadow.sh"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
